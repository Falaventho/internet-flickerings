services:
  app:
    build:
      context: .
    container_name: flicker-app
    ports:
      - 8000:8000
    volumes:
      - ./app:/app
      - ./data/web:/vol/web
    environment:
      - SECRET_KEY=${SECRET_KEY:-devsecretkey}
      - DEBUG=${DEBUG:-0}
      - DB_HOST=db
      - DB_NAME=${DB_NAME:-devdb}
      - DB_USER=${DB_USER:-rootuser}
      - DB_PASS=${DB_PASS:-changeme}
      - TEST_FLAG=${TEST_FLAG:-false}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-127.0.0.1}
      - LOCAL_SERV=${LOCAL_SERV:-true}
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    container_name: flicker-db
    environment:
      - POSTGRES_DB=${DB_NAME:-devdb}
      - POSTGRES_USER=${DB_USER:-devuser}
      - POSTGRES_PASSWORD=${DB_PASS:-changeme}
